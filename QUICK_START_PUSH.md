# ⚡ Quick Start: Push ไป GitHub

## 🎯 สิ่งที่ต้องทำ (2 ขั้นตอน)

### 1️⃣ สร้าง Personal Access Token

จากรูปที่คุณเห็น:

1. **Token name (Note):** ใส่ชื่อ เช่น `LUX Protocol Push`
2. **Expiration:** เลือก `30 days` (หรือตามต้องการ)
3. **Select scopes:** 
   - ✅ **เลือก `repo`** (Full control of private repositories) - **สำคัญ!**
   - ไม่ต้องเลือก scope อื่นๆ
4. คลิก **"Generate token"** (ปุ่มเขียว)
5. ⚠️ **คัดลอก token ทันที!** (จะเห็นแค่ครั้งเดียว)
   - Token จะเป็นแบบ: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 2️⃣ Push ไป GitHub

เปิด PowerShell ใน `protocol/` directory:

```powershell
cd protocol
git push -u origin main
```

**เมื่อถูกถาม:**
- **Username:** `aoooa147`
- **Password:** **วาง Personal Access Token ที่คัดลอกมา** (ไม่ใช่ GitHub password!)

## ✅ ตรวจสอบผลลัพธ์

ไปที่: https://github.com/aoooa147/lux-protocol

ควรเห็นไฟล์ทั้งหมดแล้ว!

---

**ง่ายๆ แค่ 2 ขั้นตอน!** 🚀

