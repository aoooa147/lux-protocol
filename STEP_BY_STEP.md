# 🚀 ขั้นตอน Push LUX Protocol ไป GitHub

## ขั้นตอนที่ 1: สร้าง Personal Access Token

จากรูปที่คุณเห็น:

1. ✅ **Token Name (Note):** ใส่ชื่อ เช่น `LUX Protocol Push`
2. ✅ **Expiration:** เลือก `30 days` หรือ `90 days` (ตามต้องการ)
3. ✅ **Select scopes:** 
   - ✅ **เลือก `repo`** (Full control of private repositories) - **สำคัญมาก!**
   - ไม่ต้องเลือก scope อื่นๆ สำหรับการ push
4. คลิก **"Generate token"** (ปุ่มเขียว)
5. ⚠️ **สำคัญ:** คัดลอก token ทันที (จะเห็นแค่ครั้งเดียว!)
   - Token จะเป็นแบบนี้: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

## ขั้นตอนที่ 2: Push ไป GitHub

เปิด PowerShell ใน `protocol/` directory แล้วรัน:

```powershell
# 1. ตรวจสอบสถานะ
git status

# 2. Push ไป GitHub
git push -u origin main
```

### เมื่อถูกถาม Username:
```
Username for 'https://github.com': aoooa147
```

### เมื่อถูกถาม Password:
```
Password for 'https://aoooa147@github.com': [วาง Personal Access Token ที่คัดลอกมา]
```

⚠️ **สำคัญ:** ใช้ **Personal Access Token** (ที่เพิ่งสร้าง) ไม่ใช่ GitHub password!

## ขั้นตอนที่ 3: ตรวจสอบผลลัพธ์

1. ไปที่: https://github.com/aoooa147/lux-protocol
2. ตรวจสอบว่าเห็นไฟล์ทั้งหมด:
   - ✅ `abis/` folder
   - ✅ `docs/` folder  
   - ✅ `README.md`
   - ✅ `LICENSE`
   - ✅ `package.json`
   - ✅ `.github/workflows/`

## 🎯 สรุปแบบสั้น

1. สร้าง Personal Access Token → เลือก `repo` scope → Generate → คัดลอก token
2. รัน `git push -u origin main`
3. Username: `aoooa147`
4. Password: **วาง token ที่คัดลอกมา** (ไม่ใช่ password!)

---

**พร้อมแล้ว! ทำตามขั้นตอนข้างบนได้เลย** ✅

